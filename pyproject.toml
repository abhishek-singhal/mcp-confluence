[project]
name = "mcp-confluence"
version = "1.0.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "bs4>=0.0.2",
    "fastmcp>=3.4.2",
    "requests>=2.34.2",
]

[project.scripts]
mcp-confluence = "mcp_confluence:main"